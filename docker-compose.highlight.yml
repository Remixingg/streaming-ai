services:
  hightlight_ai:
    build:
      context: .
      dockerfile: dockerfile.highlight
    image: highlight-ai
    container_name: highlight-ai
    ports:
      - 8001:8001
    networks:
      - caddy_net

networks:
  caddy_net:
    external: true